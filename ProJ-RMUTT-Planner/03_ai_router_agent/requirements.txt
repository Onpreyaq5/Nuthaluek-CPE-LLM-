# 03_ai_router_agent
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0

# LLM providers (ติดตั้งตาม LLM_PROVIDER ที่ใช้จริง)
google-generativeai>=0.7.0
openai>=1.30.0

# Token counting + context window
tiktoken>=0.7.0

# Retry logic
tenacity>=8.3.0

# SSE streaming
sse-starlette>=1.8.0

# /metrics ให้ Prometheus (ช่อง Monitoring ในแผนภาพ)
prometheus-fastapi-instrumentator>=7.0
