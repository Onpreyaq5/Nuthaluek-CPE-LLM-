# 07_rag_llm_engine
# หมายเหตุ: การค้นคืน (BM25 + TF-IDF) เขียนเองใน src/core/retrieval.py
# ขาเวกเตอร์เก็บและค้นใน Qdrant ผ่าน REST ด้วย httpx (src/core/qdrant_index.py)
# จึงไม่ต้องพึ่ง qdrant-client / sentence-transformers ทำให้ image เล็กและ CI รันเร็ว
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27

# /metrics ให้ Prometheus (ช่อง Monitoring ในแผนภาพ)
prometheus-fastapi-instrumentator>=7.0
